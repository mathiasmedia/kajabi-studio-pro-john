/**
 * Thin re-export. The block library + export pipeline live in the engine
 * package (`@k-studio-pro/engine`) so fixes auto-propagate to thin clients
 * via `bun update @k-studio-pro/engine`.
 */
export * from '@k-studio-pro/engine';
